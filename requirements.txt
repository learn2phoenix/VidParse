# VidParse — the parsing pipeline is CPU-only.
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12
opencv-python>=4.8
Levenshtein>=0.23
networkx>=3.0
pygraphviz>=1.11        # needs Graphviz headers: apt install libgraphviz-dev
graphviz>=0.20
XlsxWriter>=3.1
tqdm>=4.65

# Feature extraction only (step 0); not needed to parse:
#   torch>=2.0
#   torchvision>=0.15
